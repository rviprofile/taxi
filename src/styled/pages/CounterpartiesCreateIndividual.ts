import styled from '@emotion/styled'

import { Popover as MuiPopover } from 'ui'

export const CounterpartiesCreateIndividualPage = styled.div``

export const Popover = styled(MuiPopover)`
	.MuiPaper-root {
		padding: 0;
	}
`
